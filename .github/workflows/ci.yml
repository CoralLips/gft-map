name: Validate and release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm test
      - run: npm run pack:skill
      - name: Check the GitHub-installable Skill
        run: npm run test:skill -- skills/gft-map
      - name: Check the Skill built from this checkout
        run: npm run test:skill -- apps/gft-local/release/gft-map
      - uses: actions/setup-node@v4
        with:
          node-version: '22.20.0'
      - name: Prepare an isolated Agent project
        run: node -e "require('node:fs').mkdirSync('.data/skill-install', {recursive:true})"
      - name: Install with the real skills CLI
        working-directory: .data/skill-install
        run: npx --yes skills@1.7.0 add ../.. --skill gft-map --agent codex --agent claude-code --copy --yes
      - name: Check both installed Agent skills
        run: |
          npm run test:skill -- .data/skill-install/.agents/skills/gft-map
          npm run test:skill -- .data/skill-install/.claude/skills/gft-map
      - uses: actions/upload-artifact@v4
        if: runner.os == 'Linux'
        with:
          name: gft-map-skill
          path: |
            apps/gft-local/release/gft-map-*.tar.gz
            apps/gft-local/release/SHA256SUMS
          if-no-files-found: error
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/download-artifact@v4
        with:
          name: gft-map-skill
          path: artifacts
      - name: Publish versioned Skill
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          test "$RELEASE_TAG" = "v$(node -p "require('./package.json').version")"
          gh release create "$RELEASE_TAG" artifacts/* --verify-tag --title "GFT Map $RELEASE_TAG" --notes "本地主题记忆 Skill。下载并解压完整 gft-map 目录；需要 Node.js 20 或更高版本。安装和使用说明见 README。"
